#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)

{
while (password[index] -= diff_half1);
break;

{

for (index = 0; password[index]; index++)

{

if (password[index] >= (33 + diff_half2))
{
while (password[index] -= diff_half2;)
break;

}

}
}

printf("%s", password);
return (0);
}
